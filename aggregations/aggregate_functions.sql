-- Get average population of all countries
SELECT AVG(population)
FROM country;

-- Count total number of countries
SELECT COUNT(*)
FROM country;

-- Get maximum population
SELECT MAX(population)
FROM country;

-- Get minimum population
SELECT MIN(population)
FROM country;
